export function removeDuplicates(arr: number[]): number[] {
  // TODO: Retornar un nuevo arreglo sin elementos duplicados
  console.log("Hola");
  return [];
}

removeDuplicates([]);

export function filterEven(arr: number[]): number[] {
  // TODO: Retornar solo los números pares del arreglo
  return [];
}

export interface Person {
  name: string;
  age: number;
}

export function sortByAge(arr: Person[]): Person[] {
  // TODO: Ordenar los objetos por la propiedad 'age' de menor a mayor
  return [];
}

export interface Item {
  id: number;
  name: string;
}

export function findById(arr: Item[], id: number): Item | undefined {
  // TODO: Retornar el primer objeto con el id coincidente
  return undefined;
}

export function squareNumbers(arr: number[]): number[] {
  // TODO: Retornar un nuevo arreglo con los números elevados al cuadrado
  return [];
}

export function sumArray(arr: number[]): number {
  // TODO: Retornar la suma de todos los elementos
  return 0;
}
